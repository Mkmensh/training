# training repository clone helper

This folder was created for cloning the repository `https://github.com/Mkmensh/training.git`.

The repository appears to be empty, so a normal clone will create a bare repository directory with no source files.

To clone the repository here, run:

```powershell
git clone https://github.com/Mkmensh/training.git .
```
